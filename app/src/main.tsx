import React from "react";
import ReactDOM from "react-dom/client";
import PasswordRequirements from "./password-requirements.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PasswordRequirements />
  </React.StrictMode>
);
